## Requirements

### Requirement: Performance improvement SHALL be visualized with bar-based deltas
The system SHALL show improvement deltas from the first recorded performance
using bar-based visualization rather than the previous line chart view.

#### Scenario: Event analytics are rendered
- **WHEN** a visitor views swimmer event analytics
- **THEN** the system renders improvement bars, PB highlights, and benchmark
  reference lines when benchmark data is available
