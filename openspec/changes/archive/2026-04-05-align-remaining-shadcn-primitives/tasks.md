## 1. Primitive Alignment

- [x] 1.1 Replace the shared `Label` implementation with the official Radix Label-based primitive.
- [x] 1.2 Align the shared `Badge` implementation to a more standard shadcn primitive shape and add `asChild` support.

## 2. Verification

- [x] 2.1 Add or update primitive-level tests covering the aligned `Label` and `Badge` behavior.
- [x] 2.2 Run frontend lint, unit tests, and production build to ensure the primitive alignment does not regress existing pages.
- [x] 2.3 Sync the finalized primitive-alignment rule into the main OpenSpec spec and archive the change.
